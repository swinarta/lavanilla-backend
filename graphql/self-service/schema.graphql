type Query {
    products: [Product!]
    product(id: ID!): Product
}

type PriceRange {
    minVariantPrice: Float
    maxVariantPrice: Float
}

type Product {
    id: ID!
    description: String!
    priceRange: PriceRange!
    images: [URL!]
}

scalar URL