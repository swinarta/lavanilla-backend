type Query {
    ok: Boolean!
    products: [Product!]
}

type PriceRange {
    minVariantPrice: Float
    maxVariantPrice: Float
}

type Product {
    id: ID!
    description: String!
    priceRange: PriceRange!
    images: [URL!]
}

scalar URL