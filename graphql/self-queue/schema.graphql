type Query {
    presignedUrl(draftOrderId: ID!, uploadToken: String!, qty: Int!): [URL!]!
    files(draftOrderId: ID!, uploadToken: String!): [File!]
}

#type Mutation {
#    createOrder(input: OrderInput!): Order!
#}

type File {
    filename: String!
    size: Int!
    url: URL!
}

scalar URL